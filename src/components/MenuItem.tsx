interface Props {
  picture?: undefined;
  title: string;
  price: string;
  ingredients: string;
  description?: string;
}

const MenuItem: React.FC<Props> = ({
  picture,
  title,
  price,
  ingredients,
  description,
}) => {
  return null;
};

export { MenuItem };
