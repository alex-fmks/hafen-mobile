interface Props {
  picture?: undefined;
  children: React.ReactNode;
  text?: string;
}

const Container: React.FC<Props> = ({ picture, children, text }) => {
  return null;
};

export { Container };
